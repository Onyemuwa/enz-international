// The ONE place to point this site at a real backend. Leave API_BASE_URL empty
// to run in mock mode — every form works and shows success states, but nothing
// is actually sent anywhere. See ../../server/README.md to run a real backend,
// then set this to its URL (e.g. "https://api.enzinternational.com").
window.ENZ_CONFIG = {
  API_BASE_URL: '',
  WHATSAPP_NUMBER: '8613203840456',
  CONTACT_PHONE: '+86 132 0384 0456',
  CONTACT_EMAIL: 'info@enzinternational.com',
};
