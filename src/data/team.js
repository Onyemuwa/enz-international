// TODO(content): Replace with real team members before launch.
// Each entry: { name, role, bio, photo } — photo path should live in /public/images/team/.
// Left empty on purpose — publishing invented names/photos as real staff is not okay.
// The About page renders a "meet the team" placeholder state when this array is empty.
export const team = [];

export default team;
