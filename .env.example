# Copy to .env and fill in when you connect a real backend.
# Leave unset to run the site in mock mode (all forms work locally, nothing is sent anywhere).
VITE_API_BASE_URL=

# Analytics — see SETUP.md "Analytics" for where these get wired in (Phase 2).
VITE_GA4_MEASUREMENT_ID=
VITE_GTM_CONTAINER_ID=
